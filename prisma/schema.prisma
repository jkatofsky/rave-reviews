generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url = env("POSTGRES_URL")
}

// TODO: remove when working on the legit schema
model User {
  id    Int     @id @default(autoincrement())
  email String  @unique
  name  String?
}